'use client';

import SiteSearchBox from '../ui/SiteSearchBox';

/** جستجوی صفحه اصلی — زیر تیتر هیرو */
export default function HomeHeroSearch(props) {
  return <SiteSearchBox variant="hero" {...props} />;
}
